# 健康生活应用 M1 验收记录

更新日期：2026-09-08

## 离线验收

- 字段统一为 `calories_in` 和 `calorie_target`，旧别名搜索为零。
- Claude、Codex、OpenAI 兼容接口和 mock 四种驱动均经过统一结果校验。
- 四种驱动各自覆盖超时、非 JSON/非对象、`items[]` 缺字段三类错误，全部通过 `estimate()` 统一入口验证可读错误及 provider/model。
- 文字、图片、设置、确认入账、跨月范围、JSON/Markdown 报告、CLI 和网页视图的专项测试通过。
- `python3 -m pytest tests/test_wellness_* -q` 最新结果为 `70 passed`。

## 真实后端冒烟

冒烟使用无真实健康信息的固定中文餐食描述和合成餐盘图。密钥、会话 id 和本机路径不记入本文档。

当前无成功原始模型 JSON，本项不能打勾。最后一轮四次真实 CLI 返回如下：

```text
provider=claude
model=claude-haiku-4-5-20251001
input=text
EstimatorError: 估算程序返回错误：Request timed out

provider=claude
model=claude-haiku-4-5-20251001
input=image
EstimatorError: 估算程序返回错误：Request timed out

provider=codex
model=gpt-5.6-luna
input=text
ERROR: Reconnecting... waiting for network
ERROR: failed to refresh available models: Connection failed: error sending request
EstimatorError: 估算超时（超过 300 秒）

provider=codex
model=gpt-5.6-luna
input=image
ERROR: Reconnecting... waiting for network
ERROR: failed to refresh available models: Connection failed: error sending request
EstimatorError: 估算超时（超过 300 秒）
```

本轮发起 4 次真实 CLI 冒烟，成功模型返回为 0。早先调试阶段也有超时或在初始化/参数解析阶段结束的启动；无法确认其是否已产生计费请求，不冒充成功冒烟。

## 环境结论

当前 workspace-write 沙箱显式禁用网络。Codex 的 stderr 已明确记录连接失败；Claude CLI 同样在请求阶段返回超时。本机没有可用的 Claude/Codex 本地模型 daemon 或 socket。在不提权、不让宿主调度器代执行命令的边界内，本会话无法取得四份成功原始返回。

后续只需在允许两种 CLI 访问其模型服务的执行环境重跑文字/图片各一次，将脱敏后的内层 JSON 追加到本文档，再完成真调验收。
